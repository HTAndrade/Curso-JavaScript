Arrays

Possibilidade de adicionar um conjunto de valores a uma variavel;

O array deve ser escrito em colchetes, separando os valores por virgulas;

Podemos acessar um array pelo índice, por exemplo: arr[1];

lembrando que o primeiro índice é o 0, ou seja, para acessar o primeiro 
elemento: arr[0].